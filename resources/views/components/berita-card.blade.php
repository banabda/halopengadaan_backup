<div class="card-body p-2">
    <div class="mx-auto imgdiv"></div>
    <div class="d-flex my-2">
    {{-- @foreach ($tags as $tag)
        <div class="tag mr-2">{{ $tag }}</div>
    @endforeach --}}
    </div>
    <h3 class="title my-3">{{ $title }}</h3>
    <h6 class="text">{{ $detail }}</h6>
</div>
